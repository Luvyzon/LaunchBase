{% extends "site/layout.njk" %}
    {%block head%}
        <title>Receitas</title>
    {%endblock%}

    {% block header %}
        <form action="/recipes?filter=" method="get">
            <input 
            type="text"
            name="filter"
            placeholder="Buscar uma Receita"
            >
            <button class="New" type="submit">Buscar</button>
            </form>
    {% endblock header %}
    
    {% block content %}
    {% if filter %}
        <p class="filterSearch">Buscando por "{{filter}}"</p>
    {% endif %}
        <section class="cards">
         {% for recipe in recipes %}
                <div class="cardRecipe">
                    <a href="/recipe/{{recipe.id}}">
                        <img class="cardRecipeImage" src="{{recipe.image}}">
                    </a>
                    <div class="cardRecipeDescription">
                    <p>{{recipe.title}}</p>
                    <p>por {{recipe.name}}</p>
                    </div>
                    
                </div>
                <input type="hidden" name="id" value="{{chef.id}}">
            {%endfor%}
    
        </section>
    {% endblock %}