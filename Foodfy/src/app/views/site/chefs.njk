{% extends "site/layout.njk" %}

{% block head %}
<title>Chefs Foodfy</title>
{% endblock head %}

{% block content %}

<div class="listChefs">
{% for chef in chefs %}

<input type="hidden" value="{{chef.id}}">

<div class="infoChef">
   
    <div class="avatarChef">
        <a href="/chef/{{chef.id}}">
            <img src="{{chef.image}}">
        </a>
    </div>
    <div class="cardChefDescription">
        <div class="nameChef">{{chef.name}}</div>
        <p>{{chef.total}} receitas</p>
    </div>
   
</div>
{% endfor %}

</div>
</div>
{% endblock content %}
